server:
  port: 8084

spring:
  application:
    name: report-service

  datasource:
    url: jdbc:postgresql://postgres-core:5432/samt_core
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: redis
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
