# ==============================================
# SAMT MICROSERVICES - ENVIRONMENT VARIABLES
# ==============================================
# ⚠️ QUAN TRỌNG: Copy file này thành .env và thay đổi tất cả credentials!
# ⚠️ KHÔNG BAO GIỜ commit file .env vào Git!

# ============================================
#   DATABASE CREDENTIALS (BẮT BUỘC THAY ĐỔI)
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Database names
DB_IDENTITY_NAME=samt_identity
DB_CORE_NAME=samt_core
DB_PROJECTCONFIG_NAME=projectconfig_db
DB_USERGROUP_NAME=samt_usergroup

# ============================================
#   REDIS
# ============================================
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ============================================
#   APP SECRETS (REQUIRED)
# ============================================
# ProjectConfig Service encrypts Jira/GitHub tokens at rest using AES-256-GCM.
# Provide a stable 256-bit key for local dev.
# Recommended format: 64 hex chars (32 bytes) OR a strong base64 string.
ENCRYPTION_SECRET_KEY=CHANGE_ME_256_BIT_KEY

# ============================================
#   LOCAL DEV (OPTIONAL)
# ============================================
# Quick local mode: disable gRPC TLS/mTLS (DO NOT use in production).
# If you keep these unset, gRPC defaults to TLS and requires certs mounted at /certs.
GRPC_SERVER_SECURITY_ENABLED=false
GRPC_CLIENT_DEFAULT_NEGOTIATION_TYPE=plaintext

# ============================================
#   APPLICATION PORTS
# ============================================
API_GATEWAY_PORT=9080
IDENTITY_SERVICE_PORT=8081
IDENTITY_SERVICE_GRPC_PORT=9091
USER_GROUP_SERVICE_PORT=8082
USER_GROUP_SERVICE_GRPC_PORT=9095
SYNC_SERVICE_PORT=8083
PROJECT_CONFIG_SERVICE_PORT=8084
ANALYSIS_SERVICE_PORT=8087
REPORT_SERVICE_PORT=8088
NOTIFICATION_SERVICE_PORT=8085

# ============================================
#   DATABASE PORTS (Host -> Container)
# ============================================
POSTGRES_IDENTITY_PORT=5432
POSTGRES_CORE_PORT=5433
POSTGRES_PROJECTCONFIG_PORT=5434
POSTGRES_USERGROUP_PORT=5435
REDIS_PORT=6379

# ============================================
#   KAFKA
# ============================================
KAFKA_ENABLED=true
KAFKA_PORT=9092
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ============================================
#   SPRING CONFIGURATION
# ============================================
SPRING_PROFILES_ACTIVE=docker

# Datasource (được override từ docker-compose)
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}

# Redis
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379

# ============================================
#   JVM OPTIONS
# ============================================
JAVA_OPTS=-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# ============================================
#   LOGGING
# ============================================
LOG_LEVEL=INFO
SHOW_SQL=false
SQL_LOG_LEVEL=WARN

# ============================================
#   OPTIONAL: JPA/HIBERNATE
# ============================================
DDL_AUTO=validate
DB_POOL_SIZE=10
DB_POOL_MIN_IDLE=2
