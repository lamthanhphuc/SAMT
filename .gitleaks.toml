title = "SAMT Gitleaks Configuration"

[allowlist]
  description = "Allow only known non-secret placeholders"
  regexes = [
    '''BASE64_ENCODED_(PASSWORD|SECRET)''',
    '''CHANGE_ME(_[A-Z0-9_]+)?''',
    '''REPLACE_WITH_PUBLISHED_DIGEST''',
    '''__SET_VIA_DOCKER_SECRET__''',
  ]
