package com.example.user_groupservice.entity;

/**
 * Group-level roles (different from system roles).
 * Stored in user_groups table for each membership.
 */
public enum GroupRole {
    LEADER,
    MEMBER
}
