@baseUrl = http://localhost:9080
@identityBaseUrl = http://localhost:8081
@adminEmail = admin@samt.local
@adminPassword = Str0ng@Pass!
@lecturerEmail = qa.lecturer@samt.local
@lecturerPassword = Str0ng@Pass!
@studentEmail = qa.student@samt.local
@studentPassword = Str0ng@Pass!
@invalidUuid = not-a-uuid
@missingToken = invalid.jwt.token

# Prefer running regression through:
#   httpyac tests/bootstrap.http tests/fixtures.http tests/api-*.http --all
# so globals (tokens and fixture IDs) are present for generated cases.

### POST_/api/auth/login_happy
# @name POST__api_auth_login_happy
POST {{baseUrl}}/api/auth/login
Content-Type: application/json


{
  "email": "{{adminEmail}}",
  "password": "{{adminPassword}}"
}

> {%
if (response.status === 200 && response.body && response.body.accessToken) {
  client.global.set('adminAccessToken', response.body.accessToken);
}
if (response.status === 200 && response.body && response.body.refreshToken) {
  client.global.set('adminRefreshToken', response.body.refreshToken);
}
client.test("POST_/api/auth/login_happy -> [200]", function () {
  const expectedStatuses = [200];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [200]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/login_invalid_payload
# @name POST__api_auth_login_invalid_payload
POST {{baseUrl}}/api/auth/login
Content-Type: application/json


{
  "email": "",
  "password": "Str0ng@Pass!"
}

> {%
client.test("POST_/api/auth/login_invalid_payload -> [400]", function () {
  const expectedStatuses = [400];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [400]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/refresh_happy
# @name POST__api_auth_refresh_happy
POST {{baseUrl}}/api/auth/refresh
Content-Type: application/json


{
  "refreshToken": "{{adminRefreshToken}}"
}

> {%
client.test("POST_/api/auth/refresh_happy -> [200]", function () {
  const expectedStatuses = [200];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [200]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/refresh_invalid_payload
# @name POST__api_auth_refresh_invalid_payload
POST {{baseUrl}}/api/auth/refresh
Content-Type: application/json


{
  "refreshToken": ""
}

> {%
client.test("POST_/api/auth/refresh_invalid_payload -> [400]", function () {
  const expectedStatuses = [400];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [400]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/logout_happy
# @name POST__api_auth_logout_happy
POST {{baseUrl}}/api/auth/logout
Authorization: Bearer {{adminAccessToken}}
Content-Type: application/json


{
  "refreshToken": "{{adminRefreshToken}}"
}

> {%
client.test("POST_/api/auth/logout_happy -> [204]", function () {
  const expectedStatuses = [204];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [204]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/logout_unauthorized
# @name POST__api_auth_logout_unauthorized
POST {{baseUrl}}/api/auth/logout
Authorization: Bearer {{missingToken}}
Content-Type: application/json


{
  "refreshToken": "{{adminRefreshToken}}"
}

> {%
client.test("POST_/api/auth/logout_unauthorized -> [401]", function () {
  const expectedStatuses = [401];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [401]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/logout_invalid_payload
# @name POST__api_auth_logout_invalid_payload
POST {{baseUrl}}/api/auth/logout
Authorization: Bearer {{adminAccessToken}}
Content-Type: application/json


{
  "refreshToken": ""
}

> {%
client.test("POST_/api/auth/logout_invalid_payload -> [400, 401]", function () {
  const expectedStatuses = [400, 401];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [400, 401]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/register_happy
# @name POST__api_auth_register_happy
POST {{baseUrl}}/api/auth/register
Content-Type: application/json


{
  "email": "qa.student2@samt.local",
  "password": "Str0ng@Pass!",
  "confirmPassword": "Str0ng@Pass!",
  "fullName": "QA Student Two",
  "role": "STUDENT"
}

> {%
client.test("POST_/api/auth/register_happy -> [201, 409]", function () {
  const expectedStatuses = [201, 409];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [201, 409]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/register_invalid_payload
# @name POST__api_auth_register_invalid_payload
POST {{baseUrl}}/api/auth/register
Content-Type: application/json


{
  "email": "not-an-email",
  "password": "Str0ng@Pass!",
  "confirmPassword": "Str0ng@Pass!",
  "fullName": "QA api aut",
  "role": "STUDENT"
}

> {%
client.test("POST_/api/auth/register_invalid_payload -> [400]", function () {
  const expectedStatuses = [400];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [400]; body=' + (responseBody || '<empty>')
  );
});
%}

### POST_/api/auth/register_missing_required
# @name POST__api_auth_register_missing_required
POST {{baseUrl}}/api/auth/register
Content-Type: application/json


{}

> {%
client.test("POST_/api/auth/register_missing_required -> [400]", function () {
  const expectedStatuses = [400];
  const responseBody = typeof response.body === 'string' ? response.body : JSON.stringify(response.body);
  client.assert(
    expectedStatuses.includes(response.status),
    'Unexpected status ' + response.status + '; expected [400]; body=' + (responseBody || '<empty>')
  );
});
%}

