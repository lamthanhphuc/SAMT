# Autonomous QA Failure Analysis

- Stage: contract
- Endpoint: /api/project-configs/{id}
- Failure kind: missing-repository-check
- Backend code modified: no

## Candidate Files

- project-config-service/src/main/java/com/example/projectconfigservice/controller/ProjectConfigController.java
- project-config-service/src/main/java/com/example/projectconfigservice/service/ProjectConfigService.java
- project-config-service/src/main/java/com/example/projectconfigservice/exception/GlobalExceptionHandler.java

## Root Cause

The failure pattern is consistent with a lookup path that continues after a missing entity instead of converting the absence into a controlled domain response.

## Suggested Patch

```diff
@@
- ProjectConfig config = repository.findById(id).get();
+ ProjectConfig config = repository.findById(id)
+   .orElseThrow(() -> new ConfigNotFoundException("Project config not found: " + id));
```

## Pull Request Proposal

Title: [QA] Investigate contract failure on /api/project-configs/{id}

This artifact is an example proposal generated by the autonomous QA layer. Backend source code is never modified automatically by this platform.
